# ===================================
# V10.PY OPTIMIZED REQUIREMENTS - FIXED
# ===================================
# VERIFIED: Only dependencies actually used in V10.py code
# PRODUCTION READY: Minimal, efficient, fast deployment
# LOCKED VERSIONS: Prevents auto-updates that break functionality

# Core Streamlit Framework (ESSENTIAL) - LOCKED to prevent filter breaking
streamlit==1.28.0

# Data Processing & Analysis (CRITICAL) - Compatible versions
pandas>=1.5.3
numpy>=1.24.2

# Interactive Visualizations (ESSENTIAL - plotly.graph_objects used extensively)
plotly>=5.15.0

# Excel Export Functionality (CRITICAL for ExportEngine class)
xlsxwriter>=3.1.9     # Primary Excel engine used in create_excel_report()
openpyxl>=3.1.0       # Secondary Excel engine for multi-sheet exports

# HTTP Requests (if used in your V10.py)
requests>=2.28.0
